# ihcLaravel

Para rodar o sistema deve-se ter:

- PHP
- Composer

Para rodar:

- Na primeira rodagem realizar os comando:
  - composer install
  - php artisan migrate
- Depois só rodar o comando:
  - php artisan serve
